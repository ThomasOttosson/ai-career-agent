function CoverLetterRequiredNotice() {
  return (
    <p
      style={{
        padding: "8px 10px",
        border: "1px solid #f0c36d",
        borderRadius: "8px",
        background: "#fff8e1",
        fontSize: "14px",
      }}
    >
      ⚠️ Cover letter is required before approval.
    </p>
  );
}

export default CoverLetterRequiredNotice;